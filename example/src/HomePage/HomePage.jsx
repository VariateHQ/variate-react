import React from 'react'

import Hero from './Hero';

import { VariateContext } from './../../../src'

const HomePage = ({ history }) => (
  <section>
    <Hero />
    <h1>HomePage</h1>
  </section>
)

export default HomePage;
